<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Form Application</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Add a notice">
          <input type="submit" class="ghost-button" value="Add">
        </form>
        <% if(Name !== null){ %>
          <p>Added "<%= Name %>" to database.</p>
        <% } 
          if (Name == null){
          %>
          <p>Add a notice!</p>
          <%
        }
        %>
      </fieldset>
      <script type="text/javascript">console.log("it works");</script>
    </div>
  </body>
</html>